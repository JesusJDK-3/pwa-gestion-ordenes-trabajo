# Variables para Railway (servicio backend — Root Directory: backend/sistema-ot)
# Copiar estas claves en Railway → Variables

SPRING_PROFILES_ACTIVE=cloud
JWT_SECRET=genera-una-clave-aleatoria-minimo-32-caracteres
CORS_ALLOWED_ORIGIN=https://tu-frontend.vercel.app
SEED_DEMO_DATA=true
JPA_DDL_AUTO=update
VALIDACION_FOTOS_URL=http://45.71.33.77/proyecto_lima/

# PostgreSQL: al vincular el plugin Railway inyecta automáticamente:
# PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
